#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    for(int i = 0 ; i < a ; i++){
        int alpha = 65;
        for( int j = 0 ; j < i  ;j++){
            printf("%c",alpha);
        }
    }
    return 0;
}