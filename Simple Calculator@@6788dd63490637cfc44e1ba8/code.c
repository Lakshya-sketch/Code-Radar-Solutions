#include <stdio.h>

int main() {
    const char str[100];
    int a,b;
    scanf("%d %d", &a &b);
    if ( str == "+"){
        printf("%d", a + b);
    }
    else if( str == "-"{
        printf("%d", a - b);
    }
    else if( str == "*"){
        printf("%d", a * b);
    }
    else{
        printf("%d", a / b);
    }
    )
    return 0;
}