#include <stdio.h>
int main() {
    int a,count = 0;
    scanf("%d",&a);
    int arr[i];
    for( int i = 0 ; i < a ; i++){
            scanf("%d",&arr[i]);
    }
    scanf("%d",&b)
    for( int i = 0 ; i < a ; i++){
        if(arr[i] == b){
            count ++;
        }
        else{
            count=0;
        }
    }
    printf("%d",count)
    return 0;
}